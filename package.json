{
  "name": "worry-bomb-release",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "test": "node scripts/gesture-classifier.test.mjs",
    "smoke": "node scripts/smoke-test.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  }
}
